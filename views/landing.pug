extends home
block content



    <div class="flex flex-col min-h-[100vh]">

        <main class="flex-1">
            <section class="w-full py-12 md:py-24 lg:py-32 xl:py-48 ">
                <div class="container flex flex-col items-center justify-center space-y-4 px-4 md:px-6 text-center xl:space-y-10">
                    <div class="space-y-2" >
                        <h1 class="text-3xl font-bold tracking-tighter sm:text-5xl xl:text-6xl/none">
                            |Create a Professional Resume
                        </h1>
                        <p class="mx-auto max-w-[600px] text-gray-500 md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed dark:text-gray-400">
                            |Stand out with beautifully designed templates. Easily customize and download your resume.
                        </p>
                    </div>

                </div>
            </section>
                <section class="w-full py-12 md:py-24 lg:py-32 xl:py-48">



                    <div class="space-y-2 ">
                        <h1 class="text-3xl text-center font-bold tracking-tighter sm:text-5xl xl:text-6xl/none">
                            |Choose Template for your resume
                        </h1>
                        <p class="mx-auto max-w-[600px] text-gray-500 md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed dark:text-gray-400">   
                            |Stand out with beautifully designed templates. Easily customize and download your resume.
                        </p>
                    </div>
                        </div>
                <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 items-start">

                div(id='0', class="template-btn rounded-lg border bg-card text-card-foreground shadow-sm cursor-pointer",  data-v0-t="card",, data-user=`${user ? user._id : ''}`)
                    div(class="p-0" data-user=`${user ? user._id : ''}` )
                        img(src="/placeholder.svg" width="400" data-user=`${user ? user._id : ''}` height="225" alt="Image" class="aspect-16/9 object-cover")

                    div(class="flex items-center p-4 " data-user=`${user ? user._id : ''}`)
                        p(data-user=`${user ? user._id : ''}`  class="text-sm font-medium leading-none") Beach
                
                
                div(id='0' class="template-btn rounded-lg border bg-card text-card-foreground shadow-sm cursor-pointer" data-v0-t="card")
                    div(class="p-0" data-user=`${user ? user._id : ''}` )
                        img(src="/placeholder.svg" width="400" data-user=`${user ? user._id : ''}` height="225" alt="Image" class="aspect-16/9 object-cover")

                    div(class="flex items-center p-4 " data-user=`${user ? user._id : ''}`)
                        p(data-user=`${user ? user._id : ''}`  class="text-sm font-medium leading-none") Beach
                
                div(id='0' class="template-btn rounded-lg border bg-card text-card-foreground shadow-sm cursor-pointer" data-v0-t="card")
                    div(class="p-0" data-user=`${user ? user._id : ''}` )
                        img(src="/placeholder.svg" width="400" data-user=`${user ? user._id : ''}` height="225" alt="Image" class="aspect-16/9 object-cover")

                    div(class="flex items-center p-4 " data-user=`${user ? user._id : ''}`)
                        p(data-user=`${user ? user._id : ''}`  class="text-sm font-medium leading-none") Beach
                
                div(id='0' class="template-btn rounded-lg border bg-card text-card-foreground shadow-sm cursor-pointer" data-v0-t="card")
                    div(class="p-0" data-user=`${user ? user._id : ''}` )
                        img(src="/placeholder.svg" width="400" data-user=`${user ? user._id : ''}` height="225" alt="Image" class="aspect-16/9 object-cover")

                    div(class="flex items-center p-4 " data-user=`${user ? user._id : ''}`)
                        p(data-user=`${user ? user._id : ''}`  class="text-sm font-medium leading-none") Beach
                
                </div>
                </section>



        </main>

    </div>